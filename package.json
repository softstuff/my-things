{
  "name": "my-things",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.11.2",
    "@material-ui/icons": "^4.11.2",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/react-hooks": "^5.0.0",
    "@testing-library/user-event": "^12.1.10",
    "firebase": "^8.2.2",
    "flow-bin": "^0.141.0",
    "joi": "^17.3.0",
    "notistack": "^1.0.3",
    "query-string": "^6.13.7",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-hook-form": "^6.12.1",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.1",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "BROWSER=none react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "flow": "flow",
    "deploy:firestore": "firebase deploy --only firestore:rules",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy": "firebase deploy",
    "emulator": "firebase emulators:start --import emulator-snapshot --export-on-exit emulator-snapshot",
    "emulator:debgger": "firebase emulators:start --import emulator-snapshot --export-on-exit emulator-snapshot --inspect-functions 9229",
    "emulator:fresh": "firebase emulators:start --export-on-exit emulator-snapshot"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@hookform/devtools": "^2.2.1",
    "react-test-renderer": "^17.0.1"
  }
}
